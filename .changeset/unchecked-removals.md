---
'monoshot': patch
---

A line marked as removed is no longer type checked: the code being replaced is not code the snippet claims, and checking it alongside its replacement reported the conflict between the two rather than a mistake in either.
