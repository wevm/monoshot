---
'monoshot': patch
---

A line can be focused and marked at once, written as `[!code focus]` on a line of its own above it, since shiki reads one notation per line. A highlight sits lighter on the code it marks.
